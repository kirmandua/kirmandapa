<section class="section-tight">
  <div class="container">
    <div class="cta-band reveal">
      <div>
        <h3>{{ heading }}</h3>
        <p>{{ text }}</p>
      </div>
      <a href="{{ btnUrl }}" class="btn btn-primary">{{ btnLabel }}</a>
    </div>
  </div>
</section>
